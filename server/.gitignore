# =========================================================================
# .gitignore for a Node.js / Express.js Backend Project
# =========================================================================

# === Dependencies ===
# The node_modules directory contains all your project's dependencies.
# It can be very large and is easily regenerated by running `npm install` or `yarn install`.
node_modules/

# === Logs ===
# Log files are generated during runtime and should not be versioned.
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# === Environment Variables ===
# Environment variables contain sensitive information like API keys, database credentials,
# and other configuration secrets. A `.env.example` file should be committed instead
# to show which variables are required by the project.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.*.local

# Allow the example file to be checked in
!/.env.example
!/.env.template

# === Build & Distribution ===
# These directories contain the compiled or bundled output of your source code
# (e.g., from TypeScript or Babel). They are generated from your source files.
dist/
build/
.out/
.vercel/

# === Testing & Coverage ===
# Test reports, coverage data, and other testing artifacts.
coverage/
.nyc_output/
mochawesome-report/

# === IDE & Editor Configs ===
# User-specific settings for code editors and Integrated Development Environments (IDEs).
# It's common to share some project-level settings, so some files are un-ignored below.
.vscode/*
!.vscode/settings.json
!.vscode/tasks.json
!.vscode/launch.json
!.vscode/extensions.json
.idea/

# === OS-generated files ===
# System files that should not be part of the repository.
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# === Optional & Temporary Files ===
# Any other temporary or generated files.
*.tsbuildinfo
.npm/
.yarn-cache/